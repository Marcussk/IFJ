#include "stack_test.h"

#define RANDOM_NUM 15
#define REAL_NUM 17
#define IND 6
/*
void test_stack1(void) {
	int i;
	StackArr s;
	StackArr__init__(&s);
	iVal ret_iVal;
	for (i = 0; i < 8; i++)
		StackArr_push(&s, (iVal) i);

	int_StackArr_debug(&s);

	for (i = 0; i < 8; i++) {
		ret_iVal = StackArr_pop(&s);
		printf("pop : %d\n", ret_iVal.iInt);
	}

	int_StackArr_debug(&s);
	StackArr__dell__(&s);
}
*/
