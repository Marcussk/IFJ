#include "queue_tests.h"
/*tests for queueArr implementation */

void test_queue1(void) {
/*	Queue q;
	Queue__init__(&q);
	Instruction ins = { .code = i_push, .type = iInt, .a1 = NULL, .a2 = NULL,
			.dest = NULL };
	InstrParam * StackAddrend = malloc(sizeof(InstrParam));

	Queue_insert(&q, (Instruction ) { i_jmp, iVoid, NULL, NULL,
					StackAddrend });
	Queue_insert(&q, ins);
	Queue_debug(&q);
	Queue_insert(&q, ins);
	Queue_debug(&q);
	Queue_insert(&q, ins);
	Queue_debug(&q);
	Queue_insert(&q, ins);
	Queue_debug(&q);

	Queue__dell__(&q);*/
}

