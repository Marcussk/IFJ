{Test na zavorky}
var
	a : integer;
	b : real;
	c : real;
	d : real;
	e : real;
begin
	a := (0010+ 50);
	write('Hodnota a je: ', a, ''#10'');
	b := (0.1 * 10.0) * 10.0;
	write('Hodnota b je: ', b, ''#10'')

end.
