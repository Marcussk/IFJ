sudo apt-get install git meld

sudo apt-get update
sudo apt-get install openjdk-7-jdk
eclipse z http://www.eclipse.org/cdt/
potom jen chmod +x na ten spouštěcí soubor a spustí se klidně i poklepáním...

založit si účet na gitbucket (nebo být drsný/á a přihlásit se přes FB)
říctmi abych vás přidal do týmu (tj říct mi svůj nickname)

naklonovat k sobě do počítače (stáhnout )
(tj. udělám si složku IFJ v ní si otevřu terminál a napíšu git clone origin https://nic30@bitbucket.org/nic30/ifj.git )

aktualizace se stahují pomocí 
 git pull
(git pull origin master)


soubory se přidávají pomocí 
 git add jmenasouboru
 nebo přepíšnač --all

po tom co je všecko přidané se musí změny zabalit do takového jakoby balíčku (commitu)
  git commit --all

potom se to musí poslat na server:
  git push


když budem upravovat něco současně a vyskytnou se kolize musí se zmergovat to se dělá :
 git mergetool -t meld

(kdiff3|meld  -- oboje se musí doinstalovat)
(ale bude dobré se tomu vyhýbat...)

z edice git like a boss:

git pull origin master && git add --all && git commit --all && git push
 


