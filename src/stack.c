#include "stack.h"



/*
 void Stack_debug(Stack * s) {
 stackNodeT *temp = s->top;
 if (temp)
 while (temp != NULL) {
 printf("%p\n", temp->data);
 temp = temp->next;
 }
 else {
 printf("\nThe stack is empty!");
 }
 }
 */
